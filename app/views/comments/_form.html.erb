<%= form_for [@photo, @photo.comments.build] do |f| %>
	<div id="comment_new<%= @photo.id %>">
    	<%= f.text_area :msg, placeholder: 'Enter you comment' %>
  </div>


  <%= f.submit 'Комментировать', class: "btn btn-default" %>
<% end %>
